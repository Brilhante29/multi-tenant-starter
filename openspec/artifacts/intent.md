# Intent

Replace the in-memory tenancy demonstration with a runnable PostgreSQL proof: atomic catalog registration and schema provisioning, idempotent tenant migration, rollback without orphan state, isolated queries, and concurrent onboarding.

Success requires Docker-only execution, zero leakage, zero benchmark failures, and machine-readable Benchmark Result V2 evidence.
